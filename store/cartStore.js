import { createSlice } from "@reduxjs/toolkit";







// import { create } from 'zustand'




// const cartStore = create((set) => ({
//   shoppingCart: [],
//   addToCart: (item) => {
//     set(state => ({
//       shoppingCart: [...state.shoppingCart, item],
//     }));
//   },
//   removeFromCart: (itemId) => {
//     set(state => ({
//       shoppingCart: state.shoppingCart.filter(item => item.id !== itemId), // Sepetten bir ürünü kaldırın
//     }));
//   },
//   addItemToShoppingCart: (item) => {
//     set(state => ({
//       shoppingCart: [...state.shoppingCart, item], // Alışveriş sepetine yeni bir ürün ekleyin
//     }));
//   },
// }))

// export default cartStore;